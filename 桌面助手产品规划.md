# 悬浮桌面助手 / 菜单栏小工具产品规划

> 暂定名：**浮岛 FloatMate**  
> 文档版本：v0.4
> 首发平台：Windows（系统托盘 + 桌面悬浮窗），后续可扩展至 macOS 菜单栏  
> 产品定位：一个常驻桌面、足够轻量、以“看一眼、点一下”为核心交互的个人状态与工作节奏助手。

---

## 当前实现状态

FloatMate 已完成 Windows 本地可用版本，当前采用 .NET 8 WPF 实现。窗口可固定在屏幕左侧并通过 Windows AppBar 预留工作区，普通应用不会覆盖助手区域。

| 范围 | 当前状态 |
| --- | --- |
| 今日目标与详细工作内容 | 已实现 |
| 长期计划、XLSX 导入导出与甘特拖动 | 已实现 |
| 健康独立记录、提醒和历史复盘 | 已实现 |
| CPU、内存、磁盘、网络与应用使用时间 | 已实现 |
| 工作 XLSM / DOCX、健康 XLSX 独立导出 | 已实现 |
| 本地存储、托盘、开机启动与全局快捷键 | 已实现 |
| 云同步、跨设备与团队协作 | 暂不计划进入本地版本 |

## 1. 产品愿景

把分散在任务软件、系统监视器、喝水提醒、番茄钟和习惯记录工具里的高频小功能，收拢到一个低打扰的桌面入口中。

它不应该成为另一个需要认真维护的复杂应用，而应该像电脑上的一块小仪表：

- 一眼知道电脑是否健康；
- 一眼知道今天最重要的事情推进到哪里；
- 一次点击完成喝水、如厕、休息等生活记录；
- 在合适的时候轻声提醒，而不是频繁打断；
- 所有个人数据优先保存在本地，离线也能完整使用。

## 2. 目标用户

首要用户是长时间使用电脑的开发者、设计师、自由职业者和居家办公者。

典型特征：

- 每天同时推进 2～5 个目标或项目；
- 长时间坐在电脑前，容易忘记喝水、休息或活动；
- 偶尔需要查看 CPU、内存、磁盘和网络状态；
- 不愿意为简单记录打开大型应用；
- 在意资源占用、隐私和界面整洁。

## 3. 产品原则

1. **轻量常驻**：后台安静运行，资源占用可预期。
2. **一眼可读**：悬浮态只展示最重要的信息，不做复杂仪表盘。
3. **一步完成**：高频记录原则上一次点击即可完成，并支持撤销。
4. **低打扰**：提醒可合并、可延后、可静默，并感知全屏和勿扰状态。
5. **本地优先**：默认不登录、不上传；导出、同步和 AI 功能均由用户主动开启。
6. **记录而非诊断**：健康相关能力只提供习惯记录和趋势提示，不做医学诊断。

## 4. 核心使用闭环

### 4.1 开始一天

用户打开“今日面板”，建立 1～5 个今日目标，为每个目标设置名称、颜色、预计工作量或阶段节点。每个目标显示为一条“轨道”。

目标可附带独立的详细工作内容，用于记录执行步骤、交付物、注意事项和下一步；卡片默认显示摘要，编辑时在原位置展开。

### 4.2 推进工作

点击一条轨道即可将其设为“当前进行中”，开始记录专注时间。用户可以：

- 增加一个进度节点；
- 勾选目标下的子任务；
- 暂停、完成或搁置目标；
- 快速记录一句工作备注；
- 从悬浮窗启动专注计时。

### 4.3 快速照顾自己

用户通过悬浮窗的快捷按钮记录喝水、如厕、起身活动和护眼休息。记录完成后只显示轻量反馈，不弹出完整窗口。

### 4.4 结束一天

应用生成极简日结：目标完成情况、各轨道投入时间、喝水次数、久坐中断次数、系统异常时段和一条可选的今日感受。用户可一键结束当天，也可以补记或修改。

## 5. 信息架构与界面形态

### 5.1 常驻形态

产品提供三种显示层级，用户可自由组合：

| 形态 | 用途 | 默认内容 |
| --- | --- | --- |
| 系统托盘 | 最低存在感的常驻入口 | 健康度颜色、快捷菜单、打开面板 |
| 迷你悬浮球 / 胶囊 | 看一眼与快速记录 | 当前轨道、进度、CPU/内存摘要、快捷加号 |
| 展开面板 | 管理与回顾 | 今日轨道、系统详情、生活记录、提醒和日结 |

悬浮窗支持置顶、贴边吸附、自动收起、透明度调节、位置锁定和点击穿透。进入全屏应用、会议或演示时可自动隐藏。

### 5.2 展开面板导航

- **今天**：目标轨道、当前任务、快速记录、专注计时；
- **使用**：今日前台应用活跃时长、总时长与应用排行；
- **系统**：CPU、内存、磁盘、网络、电池和高占用进程；
- **回顾**：日 / 周趋势与目标完成记录；
- **设置**：显示、提醒、隐私、数据、快捷键与扩展。

## 6. 功能规划

### 6.1 系统健康度

#### 基础指标

- CPU 当前占用、短期均值与温度（硬件允许时）；
- 内存使用量、使用率与可用量；
- 磁盘占用、剩余空间和读写负载；
- 网络实时上传 / 下载速度；
- 电池电量、充电状态和续航估计（笔记本）；
- 系统已运行时间；
- 当前资源占用最高的 3 个进程。

#### 健康度表达

不只显示一个容易误导的总分，而是采用“总状态 + 原因”的方式：

- **良好**：主要指标处于正常范围；
- **注意**：某项资源持续偏高，例如“内存连续 10 分钟超过 85%”；
- **紧张**：多项资源持续高负载，或磁盘空间接近耗尽。

悬浮态用绿 / 黄 / 红色圆点表达状态，展开后显示具体原因。阈值支持自定义，并使用持续时间过滤瞬时峰值，减少误报。

#### 可扩展功能

- 异常历史时间线；
- 高占用进程快捷定位；
- 仅提醒，不默认自动结束进程；
- 游戏、编译、渲染等高负载模式，避免合理高负载触发告警；
- “今天电脑为什么变慢”摘要。

### 6.2 今日目标轨道

每个目标是一条视觉轨道，轨道状态包括：

- 未开始：灰色虚线；
- 进行中：主题色动态高亮；
- 暂停：降低饱和度；
- 受阻：显示阻塞标记和原因；
- 已完成：轨道闭合或到达终点，并触发克制的完成反馈；
- 已搁置：归入今日回顾，但不计算为失败。

每条轨道可包含：

- 目标名称与颜色；
- 可多行编辑的详细工作内容；
- 预计用时或目标节点数；
- 0～10 个子任务 / 里程碑；
- 已投入专注时间；
- 当前下一步；
- 一句话备注；
- 关联项目或标签。

建议把“完成百分比”分成三种计算方式供用户选择：手动进度、子任务完成比例、已投入时间比例。默认使用子任务比例，避免伪精确。

### 6.3 快速生活记录

#### 首发记录项

- **喝水**：默认一次 250 ml，可自定义容量；
- **如厕**：记录时间，可选类型与简短备注；
- **起身活动**：记录一次离座或拉伸；
- **护眼休息**：完成一次 20～30 秒远眺；
- **咖啡因**：咖啡 / 茶及大致份量；
- **心情与精力**：每天 1～3 次，用 1～5 级快速记录。

所有记录都支持：

- 单击使用默认值；
- 长按或右键打开详细选项；
- 5 秒内撤销；
- 自定义是否显示、排序、图标和快捷键；
- 用户自建记录项，例如吃药、散步、零食或疼痛日志。

如厕、用药、心情等敏感记录默认仅保存在本机，可单独禁止同步和导出。

### 6.4 专注与节奏

- 正计时与番茄计时两种模式；
- 计时绑定当前目标轨道；
- 自动识别电脑空闲，回来后询问是否扣除空闲时间；
- 会话结束时记录产出或下一步；
- 久坐、喝水、护眼提醒；
- 多个提醒同一时间出现时合并为一张卡片；
- 会议、全屏、屏幕共享和勿扰时自动静默；
- “下班模式”：停止工作提醒，只保留用户指定的生活提醒。

### 6.5 今日时间线与回顾

以一条统一时间线展示：

- 目标状态变化；
- 专注会话；
- 生活记录；
- 系统资源异常；
- 用户添加的简短笔记。

回顾页提供：

- 今日完成了什么；
- 时间主要投入在哪些轨道；
- 哪些目标受阻或被搁置；
- 喝水、活动与休息是否达到用户自己设定的目标；
- 本周趋势与连续记录天数；
- 可复制为 Markdown 的日记 / 工作日志。

不使用带羞耻感的“断签”语言。连续记录中断后，只展示客观数据和重新开始入口。

### 6.6 快速捕捉箱

通过全局快捷键呼出一个单行输入框，用于：

- 快速添加今日目标或子任务；
- 记下一条稍后处理的想法；
- 输入自然短语，例如“喝水 300”“完成接口文档”“25 分钟后提醒休息”；
- 将未归类内容暂存到收件箱，日结时处理。

首版自然语言解析采用本地规则，不依赖云端 AI。

### 6.7 扩展与集成（后续）

- GitHub / GitLab：提交、PR 和 Issue 可映射到目标轨道；
- 日历：根据会议自动开启静默，或把日程放入今日时间线；
- IDE：显示当前项目、分支和编码时长；
- Webhook / 本地 API：允许脚本写入事件和更新轨道；
- Obsidian / Markdown：导出每日笔记；
- 可选 AI 日结：仅在用户主动选择后，把选中的非敏感数据发送给指定服务生成总结；
- 插件系统：新增指标卡、记录类型、数据导出器和自动化规则。

## 7. 建议增加的特色功能

### 7.1 状态胶囊

悬浮胶囊一次只展示一个最相关状态，并按优先级轮换：

1. 严重系统异常；
2. 正在进行的目标与计时；
3. 已到期但未打扰的提醒；
4. CPU / 内存简报；
5. 今日喝水或活动进度。

这样可以避免把悬浮窗做成密集的小型仪表盘。

### 7.2 情境模式

提供工作、会议、深度专注、游戏、演示和休息模式。每种模式决定：

- 悬浮窗是否显示；
- 哪些提醒可以出现；
- 系统高负载阈值；
- 默认开始哪条轨道；
- 是否记录专注时间。

### 7.3 温和建议

仅基于明确规则提供可解释建议，例如：

- “你已经连续专注 92 分钟，可以起身活动一下”；
- “磁盘剩余空间低于 10%，可能影响更新和构建”；
- “这条轨道连续三天被搁置，是否缩小为一个更具体的下一步？”

每条建议都显示触发原因，并允许永久关闭同类建议。

### 7.4 隐私面板

- 清楚列出采集了哪些数据；
- 每类数据可单独开关；
- 导出按工作与健康独立分组：工作支持 XLSM 数据表和 DOCX 日报，健康支持包含汇总与明细的 XLSX；
- 一键清除指定日期或全部数据；
- 自定义数据保留天数；
- 同步或 AI 功能启用前显示精确的数据范围。

## 8. MVP 范围

第一版的目标不是功能齐全，而是验证三个核心习惯：用户是否愿意让它常驻、是否愿意每天建立轨道、是否愿意通过它完成快速记录。

### MVP 必须包含

- Windows 系统托盘；
- 可拖动、可贴边的迷你悬浮窗；
- CPU、内存、磁盘和网络实时摘要；
- 基于阈值与持续时间的系统状态提示；
- 今日目标轨道的新增、编辑、暂停、完成与搁置；
- 目标子任务与进度；
- 绑定目标的专注正计时；
- 喝水、如厕、起身、护眼四类一键记录；
- 可配置的喝水、久坐和护眼提醒；
- 今日时间线与简要日结；
- 本地数据存储、备份、导出与删除；
- 开机启动、主题、透明度和全局快捷键设置。

### MVP 暂不包含

- 账号系统与云同步；
- 移动端；
- 团队协作或排行榜；
- 云端 AI；
- 自动结束高占用进程；
- 复杂插件市场；
- 医疗结论或健康风险评分。

## 9. 关键交互示例

### 示例 A：开始目标

1. 用户点击悬浮胶囊上的目标名称；
2. 面板展开并显示今日轨道；
3. 点击“开始”后，该轨道进入进行中状态并开始计时；
4. 胶囊收起，显示“接口重构 · 00:18”。

### 示例 B：记录喝水

1. 用户点击水滴按钮；
2. 默认记录 250 ml；
3. 胶囊短暂显示“已记录 250 ml · 撤销”；
4. 两秒后恢复显示当前轨道。

### 示例 C：系统负载异常

1. 内存占用持续超过用户阈值 10 分钟；
2. 托盘状态点由绿色变为黄色；
3. 胶囊在合适时机显示“内存占用较高 · 86%”；
4. 展开后可查看趋势和高占用进程，但不会自动关闭任何程序。

## 10. 数据模型草案

| 实体 | 关键字段 |
| --- | --- |
| Goal | id、日期、标题、状态、颜色、进度模式、预计值、实际值、下一步 |
| Milestone | id、goal_id、标题、排序、完成时间 |
| FocusSession | id、goal_id、开始时间、结束时间、空闲时长、备注 |
| QuickRecord | id、类型、时间、数值、单位、备注、敏感标记 |
| SystemSample | 时间、CPU、内存、磁盘、网络、电池；按保留策略聚合或删除 |
| TimelineEvent | id、类型、时间、关联实体、摘要 |
| ReminderRule | 类型、时间 / 间隔、静默条件、启用状态 |
| WorkPlan | id、名称、周期、开始日期、结束日期、折叠状态 |
| PlanTask | id、plan_id、任务、开始日期、结束日期、状态、进度、负责人、备注 |
| AppUsage | 日期、应用标识、显示名称、前台活跃时长 |
| Preference | 显示、主题、阈值、快捷键、隐私与保留策略 |

系统采样不应无限保存。建议只保留短期明细，并将较早数据聚合成小时或日统计，防止数据库持续膨胀。

## 11. 技术方向建议

### 推荐方案

当前实现采用 **.NET 8 + WPF + Windows 原生 API + Open XML SDK**：

- WPF 负责左侧助手、设置、长期计划和历史复盘界面；
- Windows AppBar API 负责固定侧边栏并预留真实桌面工作区；
- 系统托盘、开机启动、全局快捷键和前台应用统计使用 Windows 原生能力；
- JSON 文件保存目标、健康记录、长期计划、提醒配置和应用使用时间；
- Open XML SDK 负责 XLSM/XLSX/DOCX 的本地导入导出；
- 系统指标通过低频采样和按需刷新降低资源消耗。

该技术路线无需内置浏览器运行时，适合离线、本地优先的 Windows 工具。后续如果数据量明显增长，可在保持模型兼容的前提下迁移至 SQLite。

### 轻量化策略

- 悬浮态不渲染复杂图表；
- 普通状态下每 2～5 秒采样一次，高级详情页打开时再提高刷新频率；
- 历史数据分层聚合，定期清理原始采样；
- 动画限制帧率，减少常驻 GPU 唤醒；
- 面板关闭后卸载非必要视图和图表；
- 使用事件驱动提醒，避免大量短周期定时器；
- 对插件设置 CPU、内存和调用频率限制。

### 初步性能预算

以下是研发目标，不是发布承诺：

| 指标 | 目标值 |
| --- | --- |
| 空闲 CPU | 平均低于 0.5% |
| 常驻内存 | 目标低于 80 MB，挑战目标低于 50 MB |
| 指标采样带来的 CPU 峰值 | 尽量低于 2% |
| 冷启动到托盘可用 | 2 秒内 |
| 数据库增长 | 默认一年低于 100 MB |
| 离线能力 | 核心功能 100% 可用 |

## 12. 非功能要求

### 隐私与安全

- 默认不上传数据；
- 敏感记录支持单独加密或完全排除在同步之外；
- 日志不得记录具体健康备注和输入内容；
- 外部集成遵循最小权限；
- 数据删除必须真实删除，并明确备份是否仍保留；
- 导出文件包含敏感记录时给出提示。

### 可用性

- 所有高频操作均可使用键盘；
- 支持高 DPI、多显示器和任务栏位置变化；
- 支持浅色、深色、高对比度主题；
- 颜色不是状态的唯一表达方式；
- 记录操作可撤销，避免因误点产生清理负担。

### 稳定性

- 采集单项系统指标失败时不影响其他模块；
- 异常退出后可恢复未结束的计时会话；
- 数据库迁移可回滚，并在升级前自动备份；
- 无网络时不弹错误，核心流程正常运行。

## 13. 里程碑建议

### 阶段 0：交互原型（1～2 周）

- 完成悬浮胶囊、展开面板和轨道状态的交互原型；
- 确定信息密度、窗口尺寸和快捷记录方式；
- 用假数据测试一天中的多种状态。

### 阶段 1：可用 MVP（4～6 周）

- 托盘与悬浮窗；
- 系统指标；
- 今日目标轨道；
- 专注计时；
- 四类快速记录；
- 本地数据库与设置。

### 阶段 2：日常可依赖（3～4 周）

- 提醒与静默策略；
- 时间线、日结和周回顾；
- 备份、导出、恢复；
- 性能、稳定性、多屏和高 DPI 优化。

### 阶段 3：扩展能力

- 日历、GitHub、IDE 和 Markdown 集成；
- 自定义记录类型和自动化规则；
- 可选同步与可选 AI 总结；
- macOS 菜单栏版本评估。

## 14. MVP 验收标准

- 连续运行 8 小时不出现明显内存持续增长；
- 悬浮窗不会抢夺输入焦点或干扰全屏应用；
- 用户能在 2 次点击内开始一个目标；
- 用户能在 1 次点击内完成默认喝水或如厕记录；
- 重启应用后今日目标、进度和未结束会话可恢复；
- 系统瞬时高负载不会立即触发持续告警；
- 用户可以导出并彻底删除自己的数据；
- 断网状态下所有 MVP 功能仍然可用。

## 15. 成功指标

不以“使用时长越长越好”为目标，因为该产品应尽量少占用用户注意力。

更合适的指标包括：

- 次日 / 7 日仍保持常驻的用户比例；
- 每天成功建立至少一条轨道的比例；
- 快捷记录完成率与撤销率；
- 每次交互的平均操作步数；
- 提醒被关闭、延后或永久禁用的比例；
- 空闲 CPU、常驻内存、崩溃率和数据库增长速度；
- 用户完成日结所需时间；
- 用户主观评价：“有帮助但不打扰”。

## 16. 待确认的产品决策

在进入视觉和技术原型前，建议优先确定：

1. 首发是否只支持 Windows；
2. 悬浮主体偏“胶囊”、小圆球，还是细长侧边栏；
3. 目标轨道更偏项目进度，还是更偏时间投入；
4. 如厕记录需要只记次数，还是需要类型、时长等详细字段；
5. 是否需要工作与个人数据分开的两个空间；
6. 是否接受完全本地优先，首版不提供跨设备同步；
7. 视觉风格偏克制工具感、像素风，还是治愈型桌面伙伴；
8. 是否希望它拥有角色、表情与成长系统，还是保持纯工具属性。

## 17. 推荐的首版产品定义

首版可以聚焦成一句话：

> **一个常驻桌面的轻量状态胶囊：用轨道推进今天的目标，用一次点击记录身体节奏，并在电脑真正需要关注时告诉你原因。**

这个定义能够同时覆盖工作流、生活记录与系统健康，又能约束产品不发展成笨重的“全能工作台”。
